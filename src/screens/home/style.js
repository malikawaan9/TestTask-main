import {StyleSheet} from 'react-native';
import variables from '../../util/utils';

const styles = StyleSheet.create({
  main: {flex: 1, backgroundColor: variables.colorPrimary},
});
export default styles;
